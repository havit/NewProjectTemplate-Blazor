<HxDisplayLayout @ref="hxDisplayLayout" DisplayMode="@DisplayMode">
	<HeaderTemplate>

		@(model.Id == default ? GlobalLocalizer.NewRecord : GetExchangeRateLabel(model))

	</HeaderTemplate>
	<BodyTemplate>

		<EditForm id="@nameof(ExchangeRateEdit)" Model="model">
			<DataAnnotationsValidator />

			<CurrencyPicker Label="@ExchangeRateLocalizer.Currency" @bind-Value="model.CurrencyId" />
			<HxInputDate Label="@ExchangeRateLocalizer.DateFrom" @bind-Value="model.DateFrom" ParsingErrorMessage="PEM" /> @* TODO Default ParsingErrorMessage *@
			<HxInputNumber Label="@ExchangeRateLocalizer.Rate" @bind-Value="model.Rate" ParsingErrorMessage="PEM" /> @* TODO Default ParsingErrorMessage *@

		</EditForm>

	</BodyTemplate>
	<FooterTemplate>

		<HxSubmit form="@nameof(ExchangeRateEdit)" Skin="(model.Id == default) ? ButtonSkins.Insert : ButtonSkins.Save" />

	</FooterTemplate>
</HxDisplayLayout>