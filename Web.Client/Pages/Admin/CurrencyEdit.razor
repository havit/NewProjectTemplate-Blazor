<HxDisplayLayout @ref="hxDisplayLayout" DisplayMode="DisplayMode" OnClosed="OnClosed">
    <HeaderTemplate>
        @(model.Code ?? CurrencyLocalizer.New)
    </HeaderTemplate>
    <BodyTemplate>
        <EditForm id="currency-edit" Model="model" OnValidSubmit="HandleValidSubmit">
            <DataAnnotationsValidator />
            <HxInputText Label="@CurrencyLocalizer.Code" @bind-Value="model.Code" />
            <BankAccountPicker Label="@CurrencyLocalizer.DefaultBankAccount" @bind-Value="model.DefaultBankAccountId" />
        </EditForm>
    </BodyTemplate>
    <FooterTemplate>
        <HxSubmit FormId="currency-edit" Skin="(model.Id == default) ? ButtonSkins.Insert : ButtonSkins.Save" />
    </FooterTemplate>
</HxDisplayLayout>